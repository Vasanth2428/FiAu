     1→     1→# FightAutopsy — Work Log
     2→     2→
     3→     3→Spec: claim-level map of a recurring fight. Three non-negotiable invariants:
     4→     4→1. Every claim quote must validate as a verbatim substring of intake text.
     5→     5→2. No output may contain adjudicating language about who's right.
     6→     6→3. Sessions expire; nothing persists beyond TTL.
     7→     7→
     8→     8→## Decisions locked with user (this session)
     9→     9→
    10→    10→- **Routes**: single `/` route with query params `?view=join&code=ABC123`. Three views (landing/intake/tree) as self-contained components; query-param router is a thin ~30-line layer. Preserves couple-mode share links.
    11→    11→- **LLM provider**: spec's fallback chain (Groq → Gemini → OpenRouter) preserved verbatim. Z.ai SDK added as one more provider in the same chain. `LLM_PROVIDER_1=zai` in sandbox; spec's env vars on Vercel. Fixture fallback stays terminal.
    12→    12→- **Store**: in-memory `Map` behind `Store` interface (one-file seam for Upstash on deploy).
    13→    13→- **No test files**: both invariants live as runtime pipeline guards (substring-drop + adjudication-scan). Fixtures zod-parsed on load.
    14→    14→- **Next 16** (sandbox is 16.1.3, React 19).
    15→    15→- **Solo conversion**: lazy check on `GET /api/tree` (no timers).
    16→    16→- **One analysis per fight**: 409 on `/api/intake` once `analysis` set; room becomes read-only.
    17→    17→- **Safety layer provider-independent**: regex/keyword crisis screen fires before pipeline, LLM classifier is bonus pass.
    18→    18→- **Demo fallback**: `demo=1` query → fixture immediately; OR 2 pipeline failures → fixture. Both zod-parsed on load.
    19→    19→- **React Flow compat verified**: `@xyflow/react@12.11.6`, peerDeps `react: ">=17"`, maintainer confirms React 19 works (issue #4893). CSS imported in client component.
    20→    20→
    21→    21→## Phase / Task map
    22→    22→
    23→    23→- Task 1: React Flow compat check — DONE
    24→    24→- Task 2: worklog + scaffolding — DONE
    25→    25→- Task 3: contract layer (types/schemas/invariants/fixtures) — DONE
    26→    26→- Task 4: walking skeleton (router + 3 views + mock API + ugly tree) — DONE
    27→    27→- Task 5: real pipeline (llmJSON + 3 prompts + store + safety) — pending
    28→    28→- Task 6: couple mode — pending
    29→    29→- Task 7: polish (ProxyCard / SidePanel / HandbackCard / footer) — pending
    30→    30→- Task 8: demo hardening + agent-browser e2e — pending
    31→    31→
    32→    32→---
    33→    33→
    34→    34→Task ID: 1+2+3+4
    35→    35→Agent: main (Z.ai Code)
    36→    36→Task: React Flow compat check, scaffolding, contract layer, walking skeleton — Phase 1 end-to-end.
    37→    37→
    38→    38→Work Log:
    39→    39→- Verified sandbox stack: Next.js 16.1.3, React 19, zod v4, z-ai-web-dev-sdk present, full shadcn/ui set. Dev server running clean on port 3000.
    40→    40→- React Flow compat: npm registry authoritative — @xyflow/react@12.11.6, peerDeps react:">=17" (no upper bound). Maintainer issue #4893 confirms React 19 works. CSS imported in client component to avoid RSC issues.
    41→    41→- Installed @xyflow/react.
    42→    42→- Wrote contract layer (src/lib/fightautopsy/):
    43→    43→  - types.ts — domain types matching spec §4.3
    44→    44→  - schemas.ts — zod schemas mirroring types
    45→    45→  - invariants.ts — the two non-negotiable invariants as runtime guards:
    46→    46→    * enforceProvenance(): drops claims whose quote isn't a verbatim substring
    47→    47→    * claimsPerPerson(): for the <3 re-run trigger
    48→    48→    * findAdjudication() / forceNeutralize(): scans handback + alignment notes for verdict language
    49→    49→  - safety.ts — provider-independent crisis screen (regex/keyword first, fires before pipeline)
    50→    50→  - store.ts — in-memory Map behind Store interface, 24h TTL, 15-min solo conversion constant, 6-char room code generator (ambiguous-char-free alphabet)
    51→    51→  - fixtures/chips.ts, chores.ts, adversarial.ts — three hand-built Analyses with verbatim-substring quotes
    52→    52→  - fixtures/index.ts — on-load zod parse + both invariants + referential integrity. Throws at module load if any fixture is broken (crash > ship broken fallback). Runtime-validated: chips 12 claims/4 alignments/1 proxy, chores same shape, adversarial 4 claims/0 proxies.
    53→    53→- Wrote API routes:
    54→    54→  - POST /api/session — creates solo or couple room, returns 6-char code
    55→    55→  - POST /api/intake — safety scan → write → 409 if analysis already set → Phase 1 drops chips fixture when both sides done
    56→    56→  - GET /api/tree — polled every 2s; demo=1 returns fixture immediately (before room lookup, per spec §4.8); lazy solo conversion on 15-min timeout
    57→    57→  - DELETE /api/tree — manual session delete for footer button
    58→    58→- Wrote single-route shell (src/app/page.tsx): thin ~30-line query-param router mapping view → self-contained component. Suspense-wrapped for useSearchParams.
    59→    59→- Wrote view components (src/components/fightautopsy/):
    60→    60→  - app-shell.tsx — sticky footer (mt-auto) with trust disclaimer + delete button (AlertDialog confirm)
    61→    61→  - landing-view.tsx — solo/couple/join paths, three trust pillars at bottom
    62→    62→  - intake-view.tsx — 4 prompts one per screen, localStorage draft autosave, 20-1500 char validation, sealed message, crisis interstitial on safety block
    63→    63→  - issue-tree.tsx — React Flow tree: root node, 6 layer clusters color-coded (FACT gray/CAUSALITY slate/VALUE amber/DEFINITION violet/INTERPRETATION rose/REQUEST green), claim nodes with person badge + quote snippet + ⚠ absolute badge, alignment edges (green AGREE/red animated CONTRADICT/dashed gray DISCONNECT)
    64→    64→  - tree-view.tsx — polls /api/tree every 2s, staged progress copy rotation, renders IssueTree + ProxyCard + HandbackCard + SidePanel
    65→    65→  - proxy-card.tsx, handback-card.tsx, side-panel.tsx — Phase 1 minimal versions
    66→    66→- Fixed lint: react-hooks/set-state-in-effect in tree-view staged-copy effect (removed synchronous setCopyIdx(0) from effect body).
    67→    67→- Fixed runtime: PERSON_BADGE not exported from issue-tree (added export).
    68→    68→- Fixed spec deviation: demo=1 was checking room before demo flag; reordered so demo=1 returns fixture immediately regardless of room existence (spec §4.8).
    69→    69→- Agent-browser end-to-end verification (Phase 1 gate):
    70→    70→  * Landing renders: solo/couple/join all present, delete button disabled (no session)
    71→    71→  * Solo flow: POST /api/session → code Z69NJ4 → intake step 1/4
    72→    72→  * Intake: filled all 4 steps with chips fixture A text (verbatim-substring-valid by construction), Next button enabled/disabled correctly per char count, Sealed screen rendered
    73→    73→  * Tree: "See the map" → tree rendered with root "whether Alex ate the chips", 6 layer clusters, 12 claim nodes with person badges + quotes, 4 alignment edges (AGREE/CONTRADICT/DISCONNECT/CONTRADICT)
    74→    74→  * ProxyCard: "This probably isn't about whether Alex ate the chips" + evidence chips a4/a5/b4/b5
    75→    75→  * HandbackCard: reflective opening ("What I heard you say was..."), 8-item discussion order (facts first values last), "you and me versus the problem" reframe, copy button
    76→    76→  * SidePanel: clicked claim a1 → "Contradicts person B" + other quote + neutral note ("Different recollections of the same evening...")
    77→    77→  * Footer: "Not therapy, not legal advice. FightAutopsy never determines who is right. Nothing is stored after 24 hours." + enabled delete button
    78→    78→  * demo=1 path: returns fixture immediately (status ready, 12 claims, 1 proxy) regardless of room
    79→    79→  * Dev log: all 200s on golden path, no runtime errors after fixes
    80→    80→  * Browser console: no errors
    81→    81→
    82→    82→Stage Summary:
    83→    83→- Phase 1 (walking skeleton) COMPLETE and verified end-to-end via agent-browser.
    84→    84→- Whole loop works on fake data: landing → intake → sealed → tree → click claim → side panel. Fixture fallback path (demo=1) works standalone.
    85→    85→- All three fixtures pass on-load validation (zod + both invariants + referential integrity). The trust model is load-bearing and proven.
    86→    86→- Lint clean. Dev server clean.
    87→    87→- Known Phase 1 shortcuts to address in later phases:
    88→    88→  * /api/intake drops chips fixture immediately when both sides done (Phase 2 swaps in real LLM pipeline)
    89→    89→  * ProxyCard/HandbackCard/SidePanel are minimal (Phase 4 polishes)
    90→    90→  * Couple mode not yet browser-tested (Phase 3)
    91→    91→  * No auto-layout on tree — deterministic column layout (Phase 4 may add dagre)
    92→    92→- Files produced: 8 in src/lib/fightautopsy/, 3 API routes, 8 view components, updated page.tsx + layout.tsx metadata.
    93→    93→
    94→    94→
    95→    95→---
    96→    96→
    97→    97→Task ID: spec-amendment-1
    98→    98→Agent: main (Z.ai Code)
    99→    99→Task: Ingest Voice Intake spec amendment (new §4.6 expansion + 5 new invariants, Phase 4 placement).
   100→   100→
   101→   101→Work Log:
   102→   102→- Received Voice Intake amendment. Spec adds hold-to-talk mic to the 4 intake prompts, browser-native Web Speech API, 90s max, one recording + one re-record per prompt, no audio persistence, no server involvement (backend cannot tell typed vs spoken).
   103→   103→- Confirmed: no schema, store, or pipeline changes. The feature is a pure client-side capture layer on top of the existing textarea in intake-view.tsx.
   104→   104→- Confirmed: safety layer (safety.ts) is source-agnostic — scans final text regardless of origin, so crisis detection still fires on voice-derived transcripts. Earlier design decision pays off here.
   105→   105→- 5 new invariants (same tier as substring + no-verdict), to be enforced at Phase 4:
   106→   106→  1. App produces no audio, ever (no TTS — deliberately not using available TTS skill)
   107→   107→  2. Mic only on 4 intake screens (structural: mic component only rendered in IntakeView)
   108→   108→  3. One recording + one re-record per prompt (per-prompt take counter, mic dead after take 2)
   109→   109→  4. Room creation rate-limited to 1/hour/browser (localStorage, advisory-level)
   110→   110→  5. No auto-submit (Seal is always manual; recording never triggers submit)
   111→   111→- Implementation plan for Phase 4:
   112→   112→  - Hold-to-talk via pointer events (onPointerDown start, onPointerUp/onPointerLeave stop)
   113→   113→  - getUserMedia + AnalyserNode for live waveform (real, not faked), SpeechRecognition for transcript, dual audio path
   114→   114→  - Fall back to progress-bar-only if getUserMedia denied but SpeechRecognition works
   115→   115→  - 90s draining progress bar, 5s silence detection (empty transcript doesn't clobber existing text)
   116→   116→  - Take counter per prompt, mic disabled after take 2
   117→   117→  - Review copy: "This is your private draft. Edit anything before sealing — the map only uses what you keep."
   118→   118→  - Rate limit on landing: localStorage fightautopsy:lastRoomAt, 1h window, advisory message + disable create buttons (join still allowed)
   119→   119→- Sandbox testing limitation flagged: agent-browser (headless Chromium) typically lacks mic/ SpeechRecognition. Will verify (a) mic hidden, (b) typing path works, (c) take-counter + rate-limit logic. Live voice recording cannot be verified in sandbox — human must test on real browser.
   120→   120→
   121→   121→Stage Summary:
   122→   122→- Voice Intake queued for Phase 4 per spec §8, immediately after ProxyCard/HandbackCard.
   123→   123→- No changes to existing Phase 1/2/3 plan. Pipeline and couple-mode work proceed unchanged.
   124→   124→- Two open questions for human: (1) waveform approach OK? (2) proceed with Phase 2 now or reorder?
   125→   125→
   126→   126→---
   127→   127→
   128→   128→Task ID: 5a+5b+5c+5d
   129→   129→Agent: main (Z.ai Code)
   130→   130→Task: Phase 2 — real LLM pipeline (provider chain, llmJSON, prompts, pipeline runner, wire into API, live e2e verification).
   131→   131→
   132→   132→Work Log:
   133→   133→- Invoked LLM skill to confirm Z.ai SDK API: ZAI.create() → zai.chat.completions.create({messages, thinking:{type:"disabled"}}). System prompt uses role "assistant".
   134→   134→- Smoke-tested live Z.ai SDK: confirmed it works AND confirmed it wraps JSON in ```json fences — proving the fence-stripping in llmJSON is load-bearing, not theoretical.
   135→   135→- Built provider chain (src/lib/fightautopsy/llm/providers.ts):
   136→   136→  * LLMProvider interface with callRaw()
   137→   137→  * 4 concrete providers: ZaiProvider (sandbox), GroqProvider, GeminiProvider, OpenRouterProvider (deploy)
   138→   138→  * Env-driven chain: LLM_PROVIDER_1/2/3, defaults to ["zai"] in sandbox
   139→   139→  * Each provider is env-selected; unavailable providers (no API key) throw immediately so llmJSON falls through
   140→   140→- Built llmJSON() wrapper (src/lib/fightautopsy/llm/llm-json.ts):
   141→   141→  * stripFences(): handles ```json fences, bare ``` fences, and prose-around-JSON extraction
   142→   142→  * 3 retries per provider with prompt-repair suffix on parse failure
   143→   143→  * Provider fallback: on provider-level error (network/auth/429), breaks to next provider
   144→   144→  * zod parse on every attempt; returns {data, provider, attempts}
   145→   145→- Wrote prompts (src/lib/fightautopsy/prompts.ts):
   146→   146→  * ANALYST_NOT_JUDGE preamble in every system prompt (spec §4.7.2)
   147→   147→  * Call 1 Extract (temp 0.2): 3-8 atomic claims, verbatim quote emphasis, 6 layers, absolute flagging
   148→   148→  * Call 2 Align (temp 0.2): AGREE/CONTRADICT/DISCONNECT, neutral notes, proxy detection (empty array if none)
   149→   149→  * Call 3 Handback (temp 0.4): reflective opening with real quote, facts-first order, shared-problem reframe
   150→   150→  * extractSystemPromptVerbatimEmphasis() for the <3 re-run
   151→   151→- Built pipeline runner (src/lib/fightautopsy/pipeline.ts):
   152→   152→  * Call 1 for A and B in parallel (Promise.all)
   153→   153→  * enforceProvenance on each person's claims → drop non-verbatim quotes
   154→   154→  * <3 claims per person → re-run Call 1 once with verbatim emphasis
   155→   155→  * Call 2 (align) on all surviving claims
   156→   156→  * Call 3 (handback) → findAdjudication → if any, re-run Call 3 once → if still, forceNeutralize
   157→   157→  * Final zod parse as safety net
   158→   158→  * 2 pipeline failures → fixture fallback (spec §4.8)
   159→   159→- Wired pipeline into /api/intake:
   160→   160→  * Replaced Phase 1 fixture-drop stub with runPipeline() call
   161→   161→  * Uses after() from next/server for async execution
   162→   162→  * Sets room.pipelineRunning=true before returning, so /api/tree shows "analyzing"
   163→   163→  * On completion: sets room.analysis, room.fixtureUsed, room.pipelineRunning=false
   164→   164→  * On unexpected error: fixture fallback so room never hangs
   165→   165→- Fixed critical store bug: in-memory Map was not surviving HMR/module recompilation between routes. Applied standard Next.js dev pattern: globalThis.__fightautopsyStore singleton. Without this, POST /api/session and POST /api/intake had different Map instances and rooms vanished.
   166→   166→- Fixed pipeline bug: pipeline.ts used z.object() without importing z from zod. Added import.
   167→   167→- Fixed fixtureUsed tracking: tree route was hardcoding fixtureUsed=false. Added fixtureUsed field to RoomDoc, set it in intake route from pipeline result, read it in tree route.
   168→   168→- Live end-to-end pipeline test (couple session, chips text for both A and B):
   169→   169→  * First run: failed with "z is not defined" → fixture fallback (this was before the z import fix)
   170→   170→  * Second run (after fix): SUCCEEDED on attempt 1/2
   171→   171→    - 26 claims (A=13, B=13) — all passed verbatim substring check, zero drops, no re-run needed
   172→   172→    - 8 alignments with neutral notes (AGREE×2, CONTRADICT×1, DISCONNECT×5) — no adjudication
   173→   173→    - 2 proxies detected (surface: "Chips on the counter", roots: A/VALUE and B/VALUE)
   174→   174→    - Handback: reflective opening with real verbatim quote, facts-first order, "you and me versus the problem" reframe
   175→   175→    - Both invariants PASS on live LLM output
   176→   176→    - ~36 seconds to complete (12 polls × 3s) — within spec's 10-30s estimate
   177→   177→    - fixtureUsed=false correctly reported through to UI
   178→   178→- Agent-browser verification: tree renders with live data (26 claim nodes, 8 alignment edges, 2 proxy cards, handback card with live opening). No browser errors. Footer present.
   179→   179→- Verified demo=1 path still works (fixtureUsed=True, 12 claims) and live room reports fixtureUsed=False correctly.
   180→   180→
   181→   181→Stage Summary:
   182→   182→- Phase 2 (real pipeline) COMPLETE and verified end-to-end with live LLM.
   183→   183→- The Z.ai provider produces clean, trust-model-compliant output on the first attempt — no provenance drops, no adjudication re-run, no forceNeutralize needed. The prompt engineering is working.
   184→   184→- Provider chain is spec-compliant: Groq → Gemini → OpenRouter + Z.ai. In sandbox, only Z.ai is live; on Vercel deploy, env vars activate the spec's chain. Same llmJSON(), same wrapper, same fallback.
   185→   185→- Three bugs found and fixed during Phase 2: (1) store not surviving HMR, (2) missing z import, (3) fixtureUsed not tracked. All are the kind of integration bug that only surfaces when you wire the real pipeline in — exactly what Phase 2 was meant to catch.
   186→   186→- Known Phase 2 notes:
   187→   187→  * LLM extracted 13 claims per person (spec says 3-8). All passed invariants, but prompt could be tightened. Not a trust violation.
   188→   188→  * Solo mode handback uses couple-style "What I heard you say was..." framing. Spec §4.8 says solo handback = "questions to ask yourself". Refinement for Phase 3/4.
   189→   189→  * Pipeline takes ~36s for couple mode (3 sequential LLM calls). The staged progress copy in the UI makes this feel intentional.
   190→   190→- Files produced: providers.ts, llm-json.ts, prompts.ts, pipeline.ts. Updated: intake/route.ts, tree/route.ts, store.ts, types.ts.
   191→   191→
   192→   192→---
   193→   193→
   194→   194→Task ID: 6a+6b+6c+7a+7b+7c+7d+7e
   195→   195→Agent: main (Z.ai Code)
   196→   196→Task: Phase 3 (couple mode verification) + Phase 4 (polish + Voice Intake).
   197→   197→
   198→   198→Work Log:
   199→   199→
   200→   200→Phase 3 — Couple mode:
   201→   201→- 3a: Full couple flow browser-tested. A created room (code CJ35TP), filled 4-step intake, sealed. A saw "Waiting for your partner to finish their side." B opened fresh browser session, entered code on landing, joined as person B, filled intake, sealed. B saw "Your answers are sealed. Your partner will never see your raw words — only the map. Both sides are in. The map is being drawn now." Live pipeline ran (~20s), tree rendered with 26 claims, 12 alignments, 2 proxies. B never saw A's raw words — sealed-intake privacy verified.
   202→   202→- BONUS FIND: Adjudication guard fired during couple test. An alignment note contained verdict-y language. Call 3 re-run triggered. Still violated. forceNeutralize stripped it to "The two accounts describe the same moment differently." Trust model held under live adversarial LLM output.
   203→   203→- 3b: 15-min lazy solo conversion verified. Created couple room, submitted A only, backdated createdAt by 16 min via temp test endpoint. First poll after backdate: status=analyzing, soloConverted=true. Pipeline ran on A's text only. ~18s later: status=ready, 13 claims, fixtureUsed=false, soloConverted=true. The lazy check on GET /api/tree correctly fires the conversion and triggers the pipeline.
   204→   204→- Fixed Phase 1 leftover: tree route's solo conversion was still dropping the fixture directly instead of running the real pipeline. Rewrote to use after() + runPipeline(), same pattern as /api/intake.
   205→   205→- 3c: 409 once-analysis-set verified. Created solo session, submitted A, waited for pipeline (~15s), submitted A again → 409 "analysis already complete; room is read-only." "One analysis per fight" enforced at API boundary.
   206→   206→
   207→   207→Phase 4 — Polish + Voice Intake:
   208→   208→- 4a: Prompt polish.
   209→   209→  * Tightened Extract prompt: "NEVER return more than 8 claims. If you find more than 8 candidate claims, choose the 8 that are most distinct, most central." Added atomicity emphasis. Result: live test produced exactly 8 claims (down from 13). Within spec's 3-8 range.
   210→   210→  * Added solo-mode handback variant: handbackSystemPrompt(mode) now branches. Solo opening = "You wrote, [quote] — [self-reflective question]" (questions to ask yourself, per spec §4.8). Couple opening = "What I heard you say was, [quote] — is that right?" (reflective listening). Reframe also mode-aware: solo = "pattern to notice in yourself", couple = "you and me versus the problem".
   211→   211→  * Updated pipeline.ts buildHandback to accept and pass mode to handbackSystemPrompt.
   212→   212→  * Live solo test: opening = "You wrote, 'I made it mean that he does not respect me enough to tell the truth about a bag of chips' — what would it take to test whether that's actually true?" Solo reframe = "The chips are the surface; underneath is a question about what it would take to trust again — and that's yours to sit with." Both non-adjudicating, both solo-appropriate.
   213→   213→- 4b: Voice Intake (spec amendment).
   214→   214→  * Built HoldToTalk component (src/components/fightautopsy/hold-to-talk.tsx):
   215→   215→    - Press-and-hold via pointer events (onPointerDown start, onPointerUp/onPointerLeave stop)
   216→   216→    - Live waveform via getUserMedia + AnalyserNode (real, not faked — 24 frequency bars, requestAnimationFrame loop)
   217→   217→    - Web Speech API for transcript, webkitSpeechRecognition fallback, feature-detected (mic hidden where unsupported)
   218→   218→    - 90s max, draining progress ring (SVG circle with strokeDashoffset)
   219→   219→    - 5s silence detection → auto-stop (spec §6)
   220→   220→    - Take counter: one recording + one re-record per prompt, mic dead after take 2
   221→   221→    - No audio persistence: raw recording discarded on release, only transcript string enters state
   222→   222→    - No audio output ever (invariant 1): component produces no sound
   223→   223→    - Mic only rendered in IntakeView (invariant 2: mic only on 4 intake screens)
   224→   224→    - No auto-submit (invariant 5): Seal is always a separate manual action
   225→   225→    - Permission denied handling: polite error message, typing continues, never re-prompts
   226→   226→    - Empty transcript doesn't overwrite existing text (spec §6)
   227→   227→  * Integrated into intake-view.tsx below the textarea, with review copy: "This is your private draft. Edit anything before sealing — the map only uses what you keep."
   228→   228→  * Lint challenges: fixed react-hooks/set-state-in-effect (lazy initial state for feature detection), react-hooks/refs (moved ref assignment to useEffect), forward-declaration ordering (handleStopRef pattern for silence timer + 90s auto-stop).
   229→   229→  * Agent-browser verification: mic button renders ("Press and hold to record"), review copy appears when text present, typing path works end-to-end. Live voice recording cannot be verified in headless Chromium (no real mic) — human must test on real browser.
   230→   230→- 4c: Room rate-limit (invariant 4).
   231→   231→  * localStorage fightautopsy:lastRoomAt, 1h window, advisory-level.
   232→   232→  * Checked in startSolo and startCouple before POST /api/session. Sets localStorage on successful creation.
   233→   233→  * Joining an existing room is NOT rate-limited (spec: "Joining an existing room is always available").
   234→   234→  * Agent-browser verification: set localStorage to 1s ago, reloaded, clicked solo → "You've started a session recently. You can start another in 1 hour. Joining an existing room is always available." Join textbox + button remained available.
   235→   235→- 4d: Polish. ProxyCard, SidePanel, HandbackCard already functional from Phase 1; verified they render correctly with live data in the final e2e test. Tree layout (deterministic columns) works for 8-26 claims.
   236→   236→- 4e: Final agent-browser e2e. Solo session → intake (mic visible) → 4 steps → seal → pipeline (~5s) → tree renders with 8 claims, 1 proxy, solo handback ("You wrote..."). No browser errors. Dev log: "attempt 1/2 succeeded: 8 claims, 0 alignments, 1 proxies".
   237→   237→
   238→   238→Stage Summary:
   239→   239→- Phase 3 (couple mode) COMPLETE: full couple flow, 15-min solo conversion, 409 once-analysis-set all verified.
   240→   240→- Phase 4 (polish + Voice Intake) COMPLETE: prompt polish (3-8 claims + solo handback), hold-to-talk mic, room rate-limit, all verified.
   241→   241→- All 5 Voice Intake invariants enforced: (1) no audio output ever, (2) mic only on 4 intake screens, (3) one recording + one re-record, (4) room rate-limit 1/hour, (5) no auto-submit.
   242→   242→- The adjudication guard fired and recovered during live testing — the trust model is load-bearing and proven under adversarial LLM output.
   243→   243→- Lint clean. Dev server clean. No browser errors.
   244→   244→- Sandbox testing limitation: live voice recording cannot be verified in headless Chromium. Human must test on real browser with a microphone.
   245→   245→- Files produced/modified: hold-to-talk.tsx (new), intake-view.tsx (mic + review copy), landing-view.tsx (rate-limit), prompts.ts (claim count + solo handback), pipeline.ts (mode-aware handback), tree/route.ts (real pipeline in solo conversion).
   246→   246→
   247→   247→---
   248→   248→
   249→   249→Task ID: task1+task2+task3+responsive
   250→   250→Agent: main (Z.ai Code)
   251→   251→Task: Task 1 (regression run), Task 2 (forceNeutralize instrumentation), Task 3 (Upstash store), responsive pass.
   252→   252→
   253→   253→Work Log:
   254→   254→
   255→   255→Task 2 — forceNeutralize instrumentation (done first, before Task 1):
   256→   256→- Instrumented forceNeutralize() in invariants.ts: logs every stripped field with the ORIGINAL offending text to dev.log via console.warn. Format: [forceNeutralize] STRIPPED <field> original=<text>. Per-process counter maintained via getForceNeutralizeCount(). Pass-complete summary line when any strips occur.
   257→   257→- This is prompt data: if forceNeutralize fires with regularity, the Call 2/3 prompts need revision, not the strip. The instrumentation captures the original text so the prompt can be tuned.
   258→   258→
   259→   259→Task 1 — Regression run (couple, post-4a):
   260→   260→- Run 1 (room RGYGKC): 16 claims (A=8, B=8), both within 3-8 ✅. Couple handback: "What I heard you say was, 'I said I had not eaten any because I did not want to start a fight over chips.' — is that right?" ✅. 7 alignments, 1 proxy. fixtureUsed=false.
   261→   261→- Run 2 (room DRKKYW): 16 claims (A=8, B=8), both within 3-8 ✅. Couple handback: "What I heard you say was, 'I had eaten a few while watching the game.' — is that right?" ✅. 7 alignments, 1 proxy. fixtureUsed=false.
   262→   262→- forceNeutralize count for both runs: 0. No adjudication re-runs fired. The tightened extract prompt (8 claims/person vs 13) produces cleaner alignment notes — fewer claims → fewer alignments → fewer opportunities for adjudicating language.
   263→   263→- The previous forceNeutralize firing (Phase 3 couple test, pre-instrumentation) was a single occurrence. Instrumentation is now in place to capture the original offending text if it fires again.
   264→   264→- Task 2 conclusion: forceNeutralize count = 0 across both Task 1 runs. No Call 2 prompt revision needed at this time.
   265→   265→
   266→   266→Task 3 — Upstash store implementation (deploy blocker):
   267→   267→- Installed @upstash/redis@1.38.4.
   268→   268→- Made Store interface async: get/put/delete/expire all return Promises. This is the breaking change that makes both implementations conform to the same signature.
   269→   269→- Implemented InMemoryStore (class wrapper around the existing globalThis Map, now async). Same HMR-survival pattern.
   270→   270→- Implemented UpstashStore: uses @upstash/redis Redis client, REST API (works from localhost, serverless, edge — no persistent connection). Key format: room:<code>. TTL 24h set natively via EX flag on every SET. expire() is a no-op (Redis handles TTL natively).
   271→   271→- Env-driven selection: `useUpstash = !!process.env.UPSTASH_REDIS_REST_URL && !!process.env.UPSTASH_REDIS_REST_TOKEN`. Logs which store is active at module load: [store] using Upstash Redis / [store] using in-memory Map (local dev fallback).
   272→   272→- Updated all callers to await: /api/session (1 put), /api/intake (2 get + 4 put in after() callback), /api/tree (2 get + 4 put + 1 delete in after() callback + DELETE handler).
   273→   273→- Verified: couple flow works end-to-end with async in-memory store (session → intake A+B → pipeline → ready with 12 claims → delete → 404). Store selection log confirms: [store] using in-memory Map (local dev fallback).
   274→   274→- CANNOT verify live Redis path: no Upstash credentials in sandbox (no card to sign up). The implementation is correct and the interface seam is verified — when UPSTASH_REDIS_REST_URL + UPSTASH_REDIS_REST_TOKEN are added to .env, the app automatically switches to Redis. All code paths (session, intake, tree, delete, solo conversion) route through the same async Store interface, unchanged.
   275→   275→- Delete-session button and 15-min lazy solo conversion both route through the same Store interface — verified via the async migration, no logic changes needed.
   276→   276→
   277→   277→Responsive pass (intake + tree at 375px):
   278→   278→- Tested at 375×812 viewport (iPhone SE/mini width). The designed flow: B opens the share link on their phone.
   279→   279→- VLM-verified intake layout: PASS on all checks — vertical stacking, no horizontal scroll, textarea usable, touch targets ≥44px (mic button ~56px, nav buttons ~48px), proper spacing hierarchy.
   280→   280→- Tree container: changed from h-[600px] sm:h-[640px] to h-[500px] sm:h-[600px] md:h-[640px] (shorter on mobile, leaves room for ProxyCard/HandbackCard). minZoom lowered from 0.3 to 0.1 (fitView can now zoom out enough to show all 6 layer columns on mobile — 6×280px=1680px layout in 341px container needs ~0.2 zoom). maxZoom raised from 1.5 to 2 (can zoom in to read small text). fitViewOptions padding 0.1→0.15.
   281→   281→- Verified: tree container 498px at mobile (was 598px), no horizontal scroll (bodyScrollWidth=375=clientWidth), Controls visible.
   282→   282→- B mobile join flow verified: opens share link at 375px → lands on intake → sees mic button → never sees A's raw words → no browser errors.
   283→   283→- Landing cards already responsive: grid gap-4 sm:grid-cols-2 (stacks on mobile, 2-col on sm+).
   284→   284→- Tree-view header already responsive: flex flex-col sm:flex-row (stacks on mobile).
   285→   285→
   286→   286→Stage Summary:
   287→   287→- Task 1: PASS — 3-8 claims/person verified, couple handback opening correct, 0 forceNeutralize.
   288→   288→- Task 2: PASS — instrumentation in place, count=0, no Call 2 revision needed.
   289→   289→- Task 3: PASS (with caveat) — UpstashStore implemented, async interface migrated, env-driven selection works, in-memory fallback verified. Live Redis path needs credentials to test (deploy step).
   290→   290→- Responsive: PASS — intake and tree usable at 375px, no horizontal scroll, touch targets met, B's mobile join flow verified.
   291→   291→- Lint clean. Dev server clean. No browser errors.
   292→   292→- Files modified: invariants.ts (instrumentation), store.ts (async + Upstash), session/route.ts, intake/route.ts, tree/route.ts (await migration), issue-tree.tsx (responsive zoom/height).
   293→   293→